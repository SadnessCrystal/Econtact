{% extends 'base.html.twig' %}

{% block title %}DemandeSuppression{% endblock %}

{% block body %}
    <h1>DemandeSuppression</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demande_suppression.id }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ demande_suppression.userId }}</td>
            </tr>
            <tr>
                <th>Request_date</th>
                <td>{{ demande_suppression.requestDate ? demande_suppression.requestDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ demande_suppression.reason }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demande_suppression_index') }}">back to list</a>

    <a href="{{ path('app_demande_suppression_edit', {'id': demande_suppression.id}) }}">edit</a>

    {{ include('demande_suppression/_delete_form.html.twig') }}
{% endblock %}
